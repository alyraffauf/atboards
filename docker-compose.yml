services:
  atboards:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - atboards-data:/data
    environment:
      - PUBLIC_URL=http://127.0.0.1:8000
    restart: unless-stopped

volumes:
  atboards-data:
