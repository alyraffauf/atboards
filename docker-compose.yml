services:
  atbbs:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - atbbs-data:/data
    environment:
      - PUBLIC_URL=http://127.0.0.1:8000
    restart: unless-stopped

volumes:
  atbbs-data:
